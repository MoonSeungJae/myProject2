<?xml version="1.0" encoding="UTF-8"?>
<!-- 
	1) XML (필요한 데이터를 문서로 저장) : Document,HTML (View)
		=> 문서형 데이터 베이스
		=> 운영체제의 호환성이 좋다
		=> 모든 프로그램언어에서 같은내용을 출력할 수 있다.
		=> XML
			# XML 구조 <XML>
			# 문서 제작하는 방법 (DTD,XSD)
			# 변환 (XSLT) : html,pdf
			# 언어(자바)에서 활용: DOM,SAX(*****) : XML Parser
							==	==
					메모리에 저장한상태,한줄씩 일어서 데이터 가지고 오다.
		1.구분이 되어있다.(반정형화 데이터)
			문서구조
			XML : 필요한 데이터 저장시에<a>()</a> <a href="()">
				 태그사이,속성
			ML : 태그와 속성으로 만들어진다.
				태그: 여는태그 <a> , 닫는 태그</a> , empty 태그  <input />
		2. XML: 태그는 지정된 것이 없다 ( 사용자정의) => 시멘틱으로 제작	
			1) XML 문법 
				= 태그를 만들 경우에는 
				 # 알파벳,한글을 사용한다 ( 알파벳은 대소문자를 구별한다. )
				  HTML: <A>=<a> , XML: <A> != <a>
				  java: int A; int a;
				 # 숫자를 사용할 수 있다 : 단 앞에 사용 금지 
				  <2aaa> (X) , <a2> <a3> (O)
				 # 특수문자 사용이 가능 : _ *****,- *****,., $ , :
				 <c:forEach> <file_name> 
				 # 키워드(예약어)는 사용할수가없다
				 <?xml < xml을 시행해달라 ? 는 시행해달라라는 표시
				 # 반드시 최상의 태그는 1개만 존재
				 	= 계층구조 : 전체루트 태그
				 	= 열고닫는 태그가 반드시 일치 (순서)
				 	 <a><b><c></c></b></a>
				 # 속성은 값을 지정할 때 반드시 "",'' 사용해야 한다.
				 <table width=300> << 오류
				 <table width="300"> << 정상
				 
				 
 -->
 
 <sawon> <!-- table명 최상위 태그는 테이블 명이된다. -->
 
 
  <list> <!-- Row (record) -->
   <sabun>1</sabun> <!-- Column -->
   <name>홍길동</name>
   <dept>개발부</dept>
   <sex>남자</sex>
   <age>58</age>
  </list>
  
  <list>
   <sabun>2</sabun>
   <name>심청이</name>
   <dept>인사부</dept>
   <sex>여자</sex>
   <age>99</age>
  </list>
  
  
  <list>
   <sabun>3</sabun>
   <name>박문수</name>
   <dept>사장부</dept>
   <sex>남자</sex>
   <age>8</age>
  </list>
  
  
 </sawon>
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 